<?xml version="1.0" encoding="utf-8"?>
<resources
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android 12+ 시스템 스플래시 비활성화 -->
    <style name="Theme.ShutterUp.Splash" parent="Theme.SplashScreen">
        <!-- 시스템 스플래시 완전 비활성화 -->
        <item name="android:windowSplashScreenBackground">@android:color/transparent</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>
        <item name="postSplashScreenTheme">@style/Theme.ShutterUp</item>
        
        <!-- 추가 설정으로 시스템 스플래시 즉시 종료 -->
        <item name="android:windowSplashScreenBrandingImage" tools:targetApi="s">@null</item>
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="s">@android:color/transparent</item>
    </style>

</resources>
